export const getCities = (state) => {
    return state.cities
};

export const getFetchCities = (state) => {
    return state.fetchCities
};

export const getValidCity = (state) => {
    return state.validCity
};